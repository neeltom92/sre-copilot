{{- if .Values.application.env.secrets }}
# Secret template (for reference only)
# Create the secret manually before deploying:
#
# kubectl create secret generic {{ .Values.application.env.secrets.name }} \
{{- range .Values.application.env.secrets.keys }}
#   --from-literal={{ . }}=your-api-key-here \
{{- end }}
#   -n {{ .Values.namespace }}
#
# Or use kubectl apply with base64-encoded values
{{- end }}
